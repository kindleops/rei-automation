# Build Dashboard Widget

Create a new React component in `src/modules/home/components/` that consumes a view from Supabase.
Ensure the component handles loading and error states.
Follow the `home-v2.css` styling patterns.
Register the widget in the main dashboard layout.
