node_modules
.next
dist
build
.vercel
.env
.env.*
!.env.example
.DS_Store
coverage
.turbo

# Batch execution temp files
apps/api/launch_batch_*_out.txt
apps/api/dryrun_out*.txt
apps/api/execute_launch_batch_*.mjs
apps/api/live_batch_dry_run.txt
apps/api/audit_report.txt
apps/api/audit_results.txt
apps/api/classify_tail.txt

# Local/generated audit outputs
audits/

# Local dev supervisor and MCP artifacts
.dev-all/
.playwright-mcp/
apps/dashboard/test-results/
apps/dashboard/proof/**/*.png
!apps/dashboard/proof/map-cards/*.png
apps/dashboard/proof/**/structure-evidence.txt

# Root-level local screenshots
/*.png

# Seller-engine local runtime / pilot artifacts (generated staging, shadow,
# docker-pgdata, checkpoints, reports). Source under packages/seller-engine/
# remains tracked; only var/ is disposable local state.
packages/seller-engine/var/

# One-off ops script rollback payloads (may contain local DB snapshots)
apps/api/scripts/.rollback-snapshots/
